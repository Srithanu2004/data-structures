#include<stdio.h>
int main()
{
  int arrar[100],minimum,size,c,location=1;
  printf("enter the number of elements in array\n");
  scanf("%d",&size);
  printf("enter %d integer \n",size);
  for(c=0;c<size;c++)
  scanf("%d",&array[c]);
  minimum=array[0];
  for(c=1;c<size;c++)
  {
  if(array[c]<minimum)
  {
  minimum=array[c];
  location=c+1;
  }
 }
}
output:-
enter the number of elements in array 6
enter  6 integers
2 3 1 8 7 9
minimum element is present at location 3 and its value is 1
